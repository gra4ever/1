name: Build Filarmonica NAS Docker Image

on:
  push:
    branches: [filarmonica-nas-image]
    paths:
      - 'filarmonica-backend/**'
      - '.github/workflows/build-filarmonica-nas-image.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run parser tests
        working-directory: filarmonica-backend
        run: python -m unittest -v test_parser.py test_parser_v04.py
      - name: Compile backend sources
        working-directory: filarmonica-backend
        run: python -m compileall -q app
      - name: Build linux/amd64 image
        run: docker build --platform linux/amd64 -f filarmonica-backend/Dockerfile.v09 -t stagiune-filarmonica-backend:0.9 ./filarmonica-backend
      - name: Save Docker image
        run: docker save -o stagiune-filarmonica-backend-v0.9-amd64.tar stagiune-filarmonica-backend:0.9
      - name: Show image info
        run: |
          ls -lh stagiune-filarmonica-backend-v0.9-amd64.tar
          docker image inspect stagiune-filarmonica-backend:0.9 --format '{{.Os}}/{{.Architecture}}'
      - name: Upload image tar
        uses: actions/upload-artifact@v4
        with:
          name: stagiune-filarmonica-backend-v0.9-amd64
          path: stagiune-filarmonica-backend-v0.9-amd64.tar
          if-no-files-found: error
